/**
 * Plants: a fleet-internal path named outside the quarantined module.
 *
 * The header above spans multiple lines on purpose: codeLines() must number
 * against real source lines, not the lines left after stripping comments.
 */
export const stateDir = "/fake-root/fleet-state/workers";
