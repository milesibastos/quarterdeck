// Plants: a font fetched from a font service at runtime.
import { Jost } from "next/font/google";

export const sans = Jost({ subsets: ["latin"] });
export const stylesheet = "https://fonts.googleapis.com/css2?family=Jost";
